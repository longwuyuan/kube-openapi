name: ci-pipeline

on:
  push:
    branches: [ master, issue-225 ]
  pull_request:
    branches: [ master ]

jobs:

  ci:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with :
        path: src/k8s.io/

    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: '1.16'

    - name: Build
      run: |
        echo "pwd" && pwd
        echo "content of pwd" && ls -lth src/k8s.io
        cd /home/runner/work/kube-openapi/kube-openapi/src/k8s.io/
        go mod tidy && git diff --exit-code
        go build ./cmd/... ./pkg/...

    - name: Test
      run: |
        cd /home/runner/work/kube-openapi/kube-openapi/src/k8s.io/
        GOPATH=GOPATH=${GITHUB_WORKSPACE} go test ./cmd/... ./pkg/... ./test/...
